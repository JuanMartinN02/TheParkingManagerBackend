export type updateResidentParam = {    
    resident_id: number;
    apt_number: string;   
    first_name: string;    
    last_name: string;
    email: string;
    password: string;
    vehicles_allowed: number;
    visitors_allowed: number;
}